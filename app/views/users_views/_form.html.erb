<%= form_with(model: users_view, local: true) do |form| %>
  <% if users_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_view.errors.count, "error") %> prohibited this users_view from being saved:</h2>

      <ul>
      <% users_view.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
